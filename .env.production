# Production Environment Variables (Build-time defaults)
# These are baked into the build but can be overridden at runtime via env-config.js

# API Configuration - Use relative path for same-domain monolithic deployment
VITE_API_BASE_URL=/api/ar

# Church Configuration
VITE_API_CHURCH_ID=63cd11f4808cc1923ca5f3ca

# Note: For runtime configuration flexibility, the backend should serve env-config.js
# with values injected from Docker environment variables