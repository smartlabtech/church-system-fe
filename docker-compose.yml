version: "3.8"

services:
  church-system-frontend:
    image: ghcr.io/smartlabtech/church-system-fe:latest
    container_name: church-system-fe
    restart: unless-stopped
    environment:
      # Runtime environment variables - configure these in Portainer
      - FRONTEND_API_BASE_URL=${FRONTEND_API_BASE_URL:-/api/ar}
      - FRONTEND_CHURCH_ID=${FRONTEND_CHURCH_ID:-63cd11f4808cc1923ca5f3ca}
      - FRONTEND_CHURCH_NAME_AR=${FRONTEND_CHURCH_NAME_AR:-كنيسة القديس مارمرقس}
      - FRONTEND_CHURCH_NAME_EN=${FRONTEND_CHURCH_NAME_EN:-Saint Mark Church - Maadi}
    healthcheck:
      # Frontend health check on port 80
      test: ["CMD", "curl", "-f", "http://localhost:80/"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
